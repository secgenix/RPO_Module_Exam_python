Инцидент №1: Некорректный DB_PORT в backend .env
─────────────────────────────────
СИМПТОМ:   [Что видит пользователь/инженер?] Подключение к базе данных не работает, ошибка при инициализации соединения.
ПРИЧИНА:   [Корневая причина] В файле `backend/.env` указан неверный порт базы данных (DB_PORT).
РЕШЕНИЕ:   [Какие файлы изменены, какие команды выполнены] 
           - Исправлен файл `backend/.env`: изменён параметр DB_PORT на корректный значение.
           - Исправлен файл `frontend/.env`: изменён параметр VITE_API_URL (порт).
КОММИТ:    [Хеш коммита с исправлением] 

Инцидент №2: Неверный порт в VITE_API_URL
─────────────────────────────────
СИМПТОМ:   API запросы фронтенда не достигают backend, ошибка соединения.
ПРИЧИНА:   В файле `.env` указан неверный порт в переменной VITE_API_URL.
РЕШЕНИЕ:   - Исправлен файл `.env`: изменён порт в VITE_API_URL на корректный.
КОММИТ:    

Инцидент №3: Отсутствие модуля pydantic-settings в requirements
─────────────────────────────────
СИМПТОМ:   При запуске backend возникает ошибка `ModuleNotFoundError: No module named 'pydantic_settings'`.
ПРИЧИНА:   Модуль `pydantic-settings` отсутствует в файле `requirements.txt`.
РЕШЕНИЕ:   - Добавлена строка `pydantic-settings` в файл `requirements.txt`.
КОММИТ:    

Инцидент №4: Зависимость версии sqlalchemy и psycopg2-binary блокирует установку
─────────────────────────────────
СИМПТОМ:   Ошибка при установке зависимостей: конфликт версий sqlalchemy и psycopg2-binary.
ПРИЧИНА:   В `requirements.txt` указаны жесткие версии sqlalchemy и psycopg2-binary, вызывающие конфликт.
РЕШЕНИЕ:   - Удалены версии из зависимостей `sqlalchemy` и `psycopg2-binary` в файле `requirements.txt`.
КОММИТ:    

Инцидент №5: Отсутствие файла .gitignore
─────────────────────────────────
СИМПТОМ:   В гит отслеживаются лишние файлы (.env, логи, кэш).
ПРИЧИНА:   В проекте отсутствует файл `.gitignore`.
РЕШЕНИЕ:   - Создан и добавлен файл `.gitignore` с правилами игнорирования.
КОММИТ:    

Инцидент №6: Переменные окружения в гите, отсутствует .env.example
─────────────────────────────────
СИМПТОМ:   В репозитории залиты переменные окружения из .env, нет примера конфигурации.
ПРИЧИНА:   Файл `.env` не игнорируется в гите, отсутствует файл `.env.example`.
РЕШЕНИЕ:   - Удалены переменные окружения из репозитория.
           - Создан файл `.env.example` с примером конфигурации.
           - Добавлена строка `.env` в файл `.gitignore`.
КОММИТ:    

Инцидент №7: Отсутствие логирования ошибок подключения к БД
─────────────────────────────────
СИМПТОМ:   При ошибке подключения к базе данных нет понятного сообщения, трудно диагностировать.
ПРИЧИНА:   В коде backend отсутствует логирование ошибок подключения к БД и чтение из .env.
РЕШЕНИЕ:   - Добавлено логирование ошибок подключения к БД.
           - Настроено чтение переменных из `.env` файла.
КОММИТ:    